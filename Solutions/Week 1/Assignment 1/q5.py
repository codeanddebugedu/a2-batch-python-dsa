"""
Calculate Area of Square by taking side from User
"""

side = float(input("Enter float = "))
area = side**2

print(f"Area of square with {side} side = {area}")
print(f"Area of square with {side} side = {area:.2f}")
